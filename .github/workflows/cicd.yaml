name: Building App

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

#      - name: Setup Node.js
#        uses: actions/setup-node@v2
#        with:
#          node-version: '16'

      - run: npm install
      - run: npm run e2e
      - run: npm run build
      - run: ls -la
